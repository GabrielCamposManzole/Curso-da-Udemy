// aula16/index.js
const alunos = ["luiz", "maria", "joão"];

//alunos.push("pedro"); // Adiciona "pedro" ao final do array
//alunos.unshift("ana"); // Adiciona "ana" ao início do array
//alunos.pop(); // Remove o último elemento do array
//alunos.shift(); // Remove o primeiro elemento do array
//alunos.splice(1, 1); // Remove o segundo elemento do array
//alunos[0] = "luiz"; // Modifica o primeiro elemento do array
// alunos.slice(1, 3); // Retorna uma cópia do array do índice 1 ao 2 (não inclui o 3)


// alunos.sort(); // Ordena o array em ordem alfabética
// alunos.reverse(); // Inverte a ordem dos elementos no array
// alunos.length; // Retorna o número de elementos no array
// alunos.includes("maria"); // Verifica se "maria" está no array
// alunos.indexOf("joão"); // Retorna o índice de "joão" no array
// alunos.join(", "); // Converte o array em uma string, separando os elementos por vírgula
console.log(alunos); // Output: [ 'luiz', 'maria', 'joão' ]
console.log(alunos[0]); // Output: luiz
